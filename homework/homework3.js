//1- write a coditional statement to state if hero is dc or marvel 


var name = "flash"

if (name === "flash"){
    console.log (`this character is part of the justice leage and he is in the dc universe ${name} `)
}else{
    console.log("this character is not in the dc universe")
}


//write a conditional statement taking age as variable and what year group they will be in a school (i.e 11-16 will be secondary school year group, 16-18 college anyone above optional education). (if this does not make sense please watch the summary video or the youtube video pasted above)

var number = 13;
var name = "secoundary school"

if(age ===13){
    console.log (`this person is in ${name} as their is ${age}`)
} else{
    console.log ("This person is does not attend secoundary school due to thier age")
}

// Create a shopping list of 5 items. Now make a conditional statement that checks if the list contains 5 items only. if above 5 print out error and if less than 5 print out error. ( would suggest searching greater than and less than operators)

var Shoppinglist = ["bread","butter","bannana", "peach","mango"]

if(Shoppinglist === "mango" || Shoppinglist === "peachs" || Shoppinglist==="pinapple"){
    console.log(`this item is part of my ${Shoppinglist}`)
}else{
    console.log("error")
}